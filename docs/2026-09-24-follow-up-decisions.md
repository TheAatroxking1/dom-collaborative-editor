# 后续调整记录

## 已落实：鼠标指针覆盖整个编辑区

确认与落实日期：2026-09-24。

- 鼠标覆盖整个正文编辑区（`editor-surface`），包括空文档、顶部与左右内边距、段间间距和正文下方的剩余空白。
- 离开编辑区时隐藏指针；已有的断线、失焦等生命周期规则继续保留。
- 文字输入光标继续按文本位置精确定位。
- 此项替代此前设计中“指针只在实际段落矩形内显示、段间空白隐藏”的范围限制。
- 段落内继续使用稳定段落引用与归一化坐标；非段落区域使用整个编辑区的比例坐标。不同窗口宽度或排版下，鼠标只能表达大致位置，不保证与文字光标相同的精度。
- 新增的 `surface` 指针消息需要双方加载新版页面才能识别。构建版先重新构建并重启服务，再让两端刷新；旧页面缓存尚未更新时，结束编辑后关闭全部页面再重新打开。

历史背景：在提交 `6c69db7` 上，两端实测有文字的段落、文字右侧留白、按 Enter 创建的空段落均能显示指针；段落间距和编辑器剩余空白不能显示。该显示范围限制现已按上述规则扩展，不再是待实现项。

## 已落实：连续输入与滚动后的鼠标定位

- 本机输入、页面滚动或窗口变化后，重新计算静止鼠标下方的段落或留白位置，避免沿用编辑区增高之前的旧比例坐标。
- 接收端仅显示当前屏幕内的鼠标；双方保留独立阅读位置，不自动跟随，不增加屏外方向提示。
- 离开、失焦、隐藏或断线后清除旧坐标，后续测量不会恢复旧指针。位置未变化时不重复发布 Awareness。
