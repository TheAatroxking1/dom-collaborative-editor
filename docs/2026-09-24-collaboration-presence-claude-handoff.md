# 交给 Claude：协作光标、鼠标与整段框选

用户已确认范围，请按计划实施，不再重新设计整套编辑器。当前尚未实现本轮功能。

先读：

1. [设计与交互边界](superpowers/specs/2026-09-24-collaboration-presence-selection-design.md)
2. [四步实施计划](superpowers/plans/2026-09-24-collaboration-presence-selection.md)

实际应用目录：`F:\基于DOM的协作编辑器\.claude\worktrees\confident-mclaren-506b7a`。最后核对为 `6df4770`；先查看最新提交和未提交修改，保留已有修复。外层是文档目录，不要在外层重新搭建项目。

## 用户确认的交互

- 正文内拖动仍选中文字。
- 从段落左侧留白拖动，直接框选整段，不要“框选段落”模式按钮。
- 点击选区外清除；点击正文同时正常定位文字光标。
- 复制/删除按钮点击时不能提前清掉选区；复制后保留，删除后清除。
- 支持批量复制、批量删除，一次 undo 恢复该批次。
- 选择不加锁，另一人仍能在同段输入。

## 四个独立提交

1. **文字协作光标与选区**：只加 `@tiptap/extension-collaboration-caret@3.31.3`，复用现有 Provider/Awareness。
2. **鼠标指针**：共享 Yjs 段落引用与段内归一化坐标。不同排版下显示同段大致位置，不能直接重放 clientX/clientY。
3. **段落框选**：本机矩形 + 整段高亮，远端按同一段落引用显示选中状态；不新增持久 Block ID。
4. **批量操作与撤销**：复用复制降级，一次 PM 事务删除，现有 UndoManager 前后 stopCapturing；运行完整验证并补 README。

核心新增模块预计只有 paragraphs.ts、presence.ts、paragraphSelection.ts。EditorPane 做组装和展示。必要拆分按职责进行，不为文件数量引入无意义抽象，也不把所有逻辑塞进一个大组件。

## 必须守住的边界

- 保留 Vue/Tiptap/Yjs + FastAPI/pycrdt，不重写后端，不增加 Node 服务、Redis、自研协议、账号或全局 store。
- 正文同步、IndexedDB 保存与协作撤销已经存在，本轮只接入/扩展使用。
- Awareness 是临时状态，不写正文、数据库、备份或撤销栈；不增加心跳协议。
- 选区保存段落引用，不能保存数组下标。段落前插不能选错，删除后不能转而选中邻段。
- 保留中文组合输入、粘贴、软换行、离线恢复与 HTTP LAN 手动复制。
- 保留 body、缓存前缀和数据库；启动入口仍为开发 5273 / 构建 5274。
- 不新增昵称设置页、在线用户列表、自动滚屏、触屏整段框选、拖拽排序、段落锁或另一个历史系统。

## 先前遗留问题

`6df4770` 的生产测试退出超时可能只打印日志而没有判失败。若你已修好则沿用；否则单独修失败传播并排查实际卡住原因，不把它混进功能提交，不只延长超时。保留真实日志，未解决不能宣称全部验证无异常。

## 交付证据

按四步提供可 review 的提交。最终运行 `powershell -File scripts/verify.ps1`，检查真实双上下文经 Python 同步、不同宽度/滚动、远程段落变化、批量 undo/redo、断线清理与已有离线回归。

报告核心文件、各类代码增量、实际测试结果和未实测项目。真实中文输入法、真实双设备局域网或弱网没测就明确说明。只完成本地项目，不自动推送 GitHub。
